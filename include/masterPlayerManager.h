#ifndef MASTERPLAYERMANAGER_H
#define MASTERPLAYERMANAGER_H

//#include <errno.h>
//#include <limits.h>
//#include <string.h>

//#include "shmConstants.h"

//A partir del caracter enviado por el player, lo interpreta y determina si es valido o no, y lo cuenta. 
//int interpretMovement(unsigned char mov, boardGameState *shm_bgs, int player);

//Inicializa todos los jugadores y determina su posicion inicial.
//void initializeAllPlayers(boardGameState *shm_bgs, int playerCount, pid_t * playerPids);

#endif